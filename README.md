# SkillSync Backend (skeleton)

This workspace contains a TypeScript + Express skeleton with route stubs matching the SkillSync API design.

To run in dev mode:

1. Copy `.env.example` to `.env` and adjust.
2. npm install
3. npm run dev
